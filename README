#creating a deccentralized exchange 

features :-  swap(eth to token,token to eth ,token to token)
             stake(stake FRT and eran )
             rewards(complete tasks to eran FRT token)
             
             
             

